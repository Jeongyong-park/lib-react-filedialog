import React from "react";
interface DirectoryIconProps {
    name: string;
    style?: React.CSSProperties;
    className?: string;
}
export declare const DirectoryIcon: (props: DirectoryIconProps) => React.ReactElement;
export {};
